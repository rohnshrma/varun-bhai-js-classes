
// output

// console.log("hello varun bhai"); // Prints "hello varun bhai" to the console, typically for debugging or logging purposes.

// alert("hello varun bhai"); // Displays a simple alert box with the message "hello varun bhai". Commented out, so it won't execute.

// window.alert("hello varun bhai"); // Similar to alert(), displays an alert box with the message "hello varun bhai" using the window object explicitly. Commented out, so it won't execute.

// document.write("hello world"); // Writes "hello world" directly to the HTML document, replacing its content if called after page load. Commented out, so it won't execute.

// input

// prompt("Enter your name :", "john doe"); // Displays a dialog box asking the user to input text, with "Enter your name :" as the message and "john doe" as the default input value. Commented out, so it won't execute.


// var x;
// x = 23;

// console.log(x)


// const pi;
// pi=3.14
